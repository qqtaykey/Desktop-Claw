{
  "name": "desktop-claw",
  "version": "0.0.4",
  "private": true,
  "description": "A lightweight desktop AI companion living as a floating ball on your screen. Supports macOS & Windows.",
  "license": "MIT",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "pnpm --filter @desktop-claw/desktop run dev",
    "build": "pnpm --filter @desktop-claw/desktop run build",
    "package": "pnpm --filter @desktop-claw/desktop run package",
    "test": "vitest run",
    "typecheck": "pnpm -r run typecheck",
    "lint": "pnpm -r run lint"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "devDependencies": {
    "vitest": "^2"
  }
}
